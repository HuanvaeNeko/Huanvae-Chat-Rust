# 数据库表结构设计文档

## user-access-cache（新增）

- 用途：缓存近 15 分钟签发的 Access Token，用于按设备 ID 精准拉黑。
- 触发：登录/刷新时写入；登出/删除设备时读取并批量拉黑。
- 字段：
  - `jti`：JWT 唯一标识（唯一键/主键）。
  - `user-id`：所属用户 ID（外键到 `users.user-id`）。
  - `device-id`：设备标识（与 Claims 对齐）。
  - `exp`：过期时间（Access Token 过期）。
  - `issued-at`：签发时间。
  - `inserted-at`：写入时间（默认当前时间）。
- 索引：
  - `UNIQUE(jti)`（主键）。
  - `INDEX(user-id, device-id)`（按设备查找）。
  - `INDEX(exp)`（按过期清理）。
- TTL 与清理：
  - 自动清理 `exp < now()` 或 `inserted-at < now() - 15min`。
- 读取策略：
  - 事件（登出/删除设备）读取 `user-id + device-id` 且 `exp > now()` 的所有记录，批量写入 `token-blacklist`。
- 写入策略：
  - 在 `generate_access_token`（登录与刷新）签发后写入一条，`ON CONFLICT(jti) DO NOTHING` 幂等。

## need-blacklist-check（修改说明）

- 设置为 true：
  - 在“拉黑型事件”开始阶段立即确保为 true，并将 `blacklist-check-expires-at = now + 15min`；若已为 true 则延长窗口到最新时间。
  - 事件包括：登出、删除当前设备、删除其它设备。
- 自动复位：
  - 中间件在请求时发现过期即复位；定时任务也会复位。
- 目的：
  - 窗口期内对所有受保护路由执行黑名单查询，确保被拉黑的 Access Token 立即失效。

## 📊 表名: users (用户数据表)

### 基础字段

| 字段名 | 类型 | 说明 | 存储内容结构 |
|--------|------|------|-------------|
| user-id | TEXT PRIMARY KEY | 登录id | 唯一标识 |
| user-nickname | TEXT NOT NULL | 昵称 | 用户显示名称 |
| user-password | TEXT NOT NULL | 密码 | 加密后的密码哈希 |
| user-email | TEXT UNIQUE | 邮箱 | 用户邮箱地址 |
| user-signature | TEXT DEFAULT '' | 个性签名 | 用户个性签名，可为空 |
| user-avatar-url | TEXT DEFAULT '' | 头像URL | 用户头像存储URL，格式: http://localhost:9000/avatars/{user-id}.{ext} |
| admin | TEXT | 是否是管理员 | 'true' 或 'false' |

### 好友关系字段

| 字段名 | 类型 | 说明 | 存储内容结构 |
|--------|------|------|-------------|
| user-owned-friends | TEXT | 拥有的好友列表 | friend-nickname: 好友昵称<br>friend-remark: 好友备注<br>friend-id: 好友id<br>add-time: 添加时间 |
| user-pending-friend-requests | TEXT | 等待通过的好友申请 | request-user-id: 申请用户id<br>request-message: 申请消息<br>request-time: 申请时间 |
| user-sent-friend-requests | TEXT | 发出的好友申请 | sent-to-user-id: 发送给用户id<br>sent-message: 申请消息<br>sent-time: 发送时间 |

### 群聊和对话字段

| 字段名 | 类型 | 说明 | 存储内容结构 |
|--------|------|------|-------------|
| user-joined-group-chats | TEXT | 进入的群聊 | join-time: 入群时间<br>group-name: 群名称<br>group-id: 群id<br>group-remark: 群备注 |
| user-ai-conversation-data | TEXT | 与AI的对话数据 | conversation-id: 对话id<br>conversation-title: 对话标题<br>create-time: 创建时间<br>last-message-time: 最后消息时间 |
| user-chat-data-with-friends | TEXT | 与好友的私聊数据 | message-sender-id: 消息发送人id<br>message-content: 消息内容<br>send-date: 发送日期<br>message-type: 消息类型 |

### 文件字段

| 字段名 | 类型 | 说明 | 存储内容结构 |
|--------|------|------|-------------|
| user-file-data-and-url | TEXT | 个人文件 | file-name: 文件名称<br>file-type: 文件类型<br>file-size: 文件大小<br>upload-time: 上传时间<br>file-path-url: 文件路径/URL |

### 黑名单检查控制字段

| 字段名 | 类型 | 说明 | 存储内容结构 |
|--------|------|------|-------------|
| need-blacklist-check | BOOLEAN DEFAULT false | 是否需要黑名单检查 | true: 需要检查<br>false: 跳过检查 |
| blacklist-check-expires-at | TIMESTAMP | 黑名单检查过期时间 | 15分钟后自动过期<br>过期后自动关闭检查 |

#### 使用说明

此字段用于智能性能优化，在以下安全事件时启用：
- 用户修改密码
- 远程登出其他设备
- 检测到 Token 泄露

**工作原理：**
1. 正常情况下 `need-blacklist-check = false`，跳过黑名单查询（高性能）
2. 安全事件发生时设置 `need-blacklist-check = true`，启用黑名单检查（15分钟）
3. 15分钟后自动恢复为 `false`（定时任务清理）

---

## 📊 表名: user-refresh-tokens (用户刷新Token管理表)

### 用途
管理多设备登录，每个设备独立的 Refresh Token（7天有效期）

### 字段设计

| 字段名 | 类型 | 说明 | 备注 |
|--------|------|------|------|
| token-id | TEXT PRIMARY KEY | Token唯一ID | UUID v7 |
| user-id | TEXT NOT NULL | 用户ID | 外键关联 users.user-id |
| refresh-token | TEXT NOT NULL UNIQUE | 刷新Token | JWT格式，7天有效 |
| device-id | TEXT NOT NULL | 设备唯一标识 | 基于设备信息生成的哈希 |
| device-info | TEXT | 设备详细信息 | JSON格式存储 |
| ip-address | TEXT | 登录IP地址 | 用于安全监控 |
| created-at | TIMESTAMP | 创建时间 | Token签发时间 |
| expires-at | TIMESTAMP NOT NULL | 过期时间 | 7天后 |
| last-used-at | TIMESTAMP | 最后使用时间 | 每次刷新时更新 |
| is-revoked | BOOLEAN DEFAULT false | 是否已撤销 | 登出时设为true |
| revoked-at | TIMESTAMP | 撤销时间 | 撤销时记录 |
| revoked-reason | TEXT | 撤销原因 | user_logout, security_issue等 |

### device-info 字段存储格式（JSON）

```json
{
  "device_type": "mobile",           // "mobile", "desktop", "tablet"
  "os": "Android 14",                // 操作系统
  "browser": "Chrome 120",           // 浏览器
  "mac_address": "00:1A:2B:3C:4D:5E",// MAC地址（可选）
  "device_name": "Pixel 8 Pro",      // 设备名称
  "user_agent": "Mozilla/5.0..."     // 完整User-Agent
}
```

### 索引

- `idx-refresh-tokens-user-id` - 用于查询用户的所有设备
- `idx-refresh-tokens-token` - 用于快速验证Token
- `idx-refresh-tokens-expires` - 用于清理过期Token
- `idx-refresh-tokens-device` - 用于设备管理

---

## 📊 表名: token-blacklist (Token黑名单表)

### 用途
紧急撤销 Access Token（15分钟有效期内需要立即失效的情况）

### 字段设计

| 字段名 | 类型 | 说明 | 备注 |
|--------|------|------|------|
| jti | TEXT PRIMARY KEY | Token的jti字段 | JWT中的唯一标识 |
| user-id | TEXT NOT NULL | 用户ID | 便于查询用户的所有黑名单Token |
| token-type | TEXT | Token类型 | "access" 或 "refresh" |
| expires-at | TIMESTAMP NOT NULL | Token过期时间 | 与原Token一致 |
| blacklisted-at | TIMESTAMP DEFAULT CURRENT_TIMESTAMP | 加入黑名单时间 | |
| reason | TEXT | 撤销原因 | 安全事件、密码修改等 |

### 索引

- `idx-blacklist-jti` - 主键索引
- `idx-blacklist-user-id` - 用于查询用户黑名单
- `idx-blacklist-expires` - 用于定期清理过期记录

### 清理策略

定期删除已过期的黑名单记录（因为过期的Token本身已经无效）：
DELETE FROM "token-blacklist" WHERE "expires-at" < NOW();

---

## 📊 表名: groups (群聊数据表)

### 基础字段

| 字段名 | 类型 | 说明 | 存储内容结构 |
|--------|------|------|-------------|
| group-id | TEXT PRIMARY KEY | 群聊id | 唯一标识 |
| group-name | TEXT NOT NULL | 群名称 | 群聊显示名称 |

### 成员管理字段

| 字段名 | 类型 | 说明 | 存储内容结构 |
|--------|------|------|-------------|
| group-members | TEXT | 群成员 | member-id: 群成员id<br>member-nickname: 成员昵称<br>join-time: 加入时间<br>member-role: 成员角色 |
| group-administrators | TEXT | 群管理员 | admin-id: 管理id<br>admin-nickname: 管理昵称<br>promote-time: 被设立为管理的时间<br>admin-permissions: 管理权限 |
| group-owner | TEXT | 群主 | owner-id: 群主id<br>owner-nickname: 群主昵称<br>group-create-time: 创建群聊的时间<br>become-owner-time: 成为群主的时间 |

### 群聊内容字段

| 字段名 | 类型 | 说明 | 存储内容结构 |
|--------|------|------|-------------|
| group-chat-data | TEXT | 群聊天信息 | message-sender-id: 消息发送人id<br>message-content: 消息内容<br>send-date: 发送日期<br>message-type: 消息类型<br>reply-to: 回复的消息id |
| group-muted-members | TEXT | 禁言人员 | muted-user-id: 被禁言用户id<br>mute-reason: 禁言原因<br>mute-expire-time: 禁言到期时间<br>mute-by: 执行禁言的管理员id |
| group-notice | TEXT | 群通知 | notice-title: 通知标题<br>notice-content: 通知内容<br>notice-publish-time: 通知发布时间<br>notice-publisher-id: 发布者id |
| group-chat-file-data-and-url | TEXT | 群聊文件 | file-name: 文件名称<br>file-type: 文件类型<br>file-size: 文件大小<br>upload-time: 上传时间<br>file-path-url: 文件路径/URL<br>uploader-id: 上传者id |

---

## 📊 表名: friend-messages (好友消息表)

### 用途
存储用户之间的所有聊天消息，支持文本、图片、视频、文件等多种消息类型。

### 字段设计

| 字段名 | 类型 | 说明 | 备注 |
|--------|------|------|------|
| message-uuid | TEXT PRIMARY KEY | 消息唯一标识 | UUID v4 |
| conversation-uuid | TEXT NOT NULL | 会话唯一标识 | 双方用户ID排序后组合 |
| sender-id | TEXT NOT NULL | 发送者ID | 外键关联 users.user-id |
| receiver-id | TEXT NOT NULL | 接收者ID | 外键关联 users.user-id |
| message-content | TEXT NOT NULL | 消息内容 | 文本消息的内容，媒体消息可为描述 |
| message-type | TEXT NOT NULL | 消息类型 | text/image/video/file |
| file-url | TEXT | 文件URL | 媒体消息时存储MinIO URL |
| file-size | BIGINT | 文件大小 | 字节为单位 |
| send-time | TIMESTAMP NOT NULL | 发送时间 | UTC时间，服务器生成 |
| is-deleted-by-sender | BOOLEAN DEFAULT false | 发送者是否删除 | 软删除标记 |
| is-deleted-by-receiver | BOOLEAN DEFAULT false | 接收者是否删除 | 软删除标记 |

### conversation-uuid 生成规则

将两个用户ID排序后组合，确保双方查询同一会话：

```rust
// 示例代码
fn generate_conversation_uuid(user_id_1: &str, user_id_2: &str) -> String {
    let mut ids = vec![user_id_1, user_id_2];
    ids.sort();
    format!("conv-{}-{}", ids[0], ids[1])
}

// 示例结果
// user-123 和 user-456 → "conv-user-123-user-456"
// user-456 和 user-123 → "conv-user-123-user-456" (相同)
```

### 索引

- `idx-messages-conversation-time` - 会话+时间复合索引（主查询）
- `idx-messages-sender` - 发送者索引
- `idx-messages-receiver` - 接收者索引
- `idx-messages-send-time` - 发送时间索引
- `idx-messages-conversation` - 会话索引

### 软删除说明

- 每个用户可以独立删除消息，不影响对方查看
- 查询时需要根据当前用户过滤已删除的消息
- 示例查询：
  ```sql
  SELECT * FROM "friend-messages"
  WHERE "conversation-uuid" = $1
    AND (
      ("is-deleted-by-sender" = false AND "sender-id" = $2) OR
      ("is-deleted-by-receiver" = false AND "receiver-id" = $2)
    )
  ORDER BY "send-time" DESC
  LIMIT 50;
  ```

---

## 📊 表名: friend-unread-messages (未读消息表)

### 用途
存储每个用户与好友的会话信息和未读消息计数，用于会话列表展示和未读角标。

### 字段设计

| 字段名 | 类型 | 说明 | 备注 |
|--------|------|------|------|
| unread-id | TEXT PRIMARY KEY | 记录唯一标识 | UUID |
| user-id | TEXT NOT NULL | 用户ID | 外键关联 users.user-id |
| conversation-uuid | TEXT NOT NULL | 会话唯一标识 | 与消息表对应 |
| friend-id | TEXT NOT NULL | 好友ID | 用于显示好友信息 |
| unread-count | INTEGER DEFAULT 0 | 未读消息数量 | 显示角标 |
| last-message-uuid | TEXT | 最后一条消息UUID | 用于分页查询 |
| last-message-content | TEXT | 最后一条消息内容 | 会话列表预览 |
| last-message-type | TEXT | 最后一条消息类型 | text/image/video/file |
| last-message-time | TIMESTAMP | 最后消息时间 | UTC时间 |
| updated-at | TIMESTAMP | 更新时间 | 自动更新 |

### 索引

- `idx-unread-user-updated` - 用户+更新时间（会话列表查询）
- `idx-unread-conversation` - 会话索引
- `idx-unread-count` - 未读数索引（部分索引，仅索引 unread-count > 0）

### 唯一约束

- `(user-id, conversation-uuid)` - 每个用户对每个会话只有一条记录

### 更新策略

**收到新消息时:**
```sql
INSERT INTO "friend-unread-messages" (
    "unread-id", "user-id", "conversation-uuid", "friend-id",
    "unread-count", "last-message-uuid", "last-message-content",
    "last-message-type", "last-message-time"
)
VALUES ($1, $2, $3, $4, 1, $5, $6, $7, $8)
ON CONFLICT ("user-id", "conversation-uuid")
DO UPDATE SET
    "unread-count" = "friend-unread-messages"."unread-count" + 1,
    "last-message-uuid" = $5,
    "last-message-content" = $6,
    "last-message-type" = $7,
    "last-message-time" = $8,
    "updated-at" = CURRENT_TIMESTAMP;
```

**标记已读时:**
```sql
UPDATE "friend-unread-messages"
SET "unread-count" = 0,
    "updated-at" = CURRENT_TIMESTAMP
WHERE "user-id" = $1 AND "conversation-uuid" = $2;
```

### 会话列表查询示例

```sql
SELECT 
    u."unread-id",
    u."conversation-uuid",
    u."friend-id",
    usr."user-nickname" AS friend_nickname,
    usr."user-avatar-url" AS friend_avatar,
    u."unread-count",
    u."last-message-content",
    u."last-message-type",
    u."last-message-time"
FROM "friend-unread-messages" u
LEFT JOIN "users" usr ON usr."user-id" = u."friend-id"
WHERE u."user-id" = $1
ORDER BY u."last-message-time" DESC
LIMIT 50;
```

---

## 📊 表名: groups (群聊数据表)

### 基础字段

| 字段名 | 类型 | 说明 | 存储内容结构 |
|--------|------|------|-------------|
| group-id | TEXT PRIMARY KEY | 群聊id | 唯一标识 |
| group-name | TEXT NOT NULL | 群名称 | 群聊显示名称 |

### 成员管理字段

| 字段名 | 类型 | 说明 | 存储内容结构 |
|--------|------|------|-------------|
| group-members | TEXT | 群成员 | member-id: 群成员id<br>member-nickname: 成员昵称<br>join-time: 加入时间<br>member-role: 成员角色 |
| group-administrators | TEXT | 群管理员 | admin-id: 管理id<br>admin-nickname: 管理昵称<br>promote-time: 被设立为管理的时间<br>admin-permissions: 管理权限 |
| group-owner | TEXT | 群主 | owner-id: 群主id<br>owner-nickname: 群主昵称<br>group-create-time: 创建群聊的时间<br>become-owner-time: 成为群主的时间 |

### 群聊内容字段

| 字段名 | 类型 | 说明 | 存储内容结构 |
|--------|------|------|-------------|
| group-chat-data | TEXT | 群聊天信息 | message-sender-id: 消息发送人id<br>message-content: 消息内容<br>send-date: 发送日期<br>message-type: 消息类型<br>reply-to: 回复的消息id |
| group-muted-members | TEXT | 禁言人员 | muted-user-id: 被禁言用户id<br>mute-reason: 禁言原因<br>mute-expire-time: 禁言到期时间<br>mute-by: 执行禁言的管理员id |
| group-notice | TEXT | 群通知 | notice-title: 通知标题<br>notice-content: 通知内容<br>notice-publish-time: 通知发布时间<br>notice-publisher-id: 发布者id |
| group-chat-file-data-and-url | TEXT | 群聊文件 | file-name: 文件名称<br>file-type: 文件类型<br>file-size: 文件大小<br>upload-time: 上传时间<br>file-path-url: 文件路径/URL<br>uploader-id: 上传者id |

---

## 📝 数据存储格式说明

### TEXT 字段存储约定

所有复合数据以**逗号分隔的键值对**形式存储，格式：`key1:value1,key2:value2`

#### 示例：

```
# user-owned-firends 字段存储示例：
friend-nickname:张三,friend-remark:老同学,friend-id:user-123,add-time:2025-01-15T10:30:00

# group-members 字段存储示例：
member-id:user-001,member-nickname:李四,join-time:2025-01-10T08:00:00,member-role:member
```

### 多条记录存储

使用**分号(;)**分隔多条记录：

```
# 多个好友存储示例：
friend-nickname:张三,friend-remark:老同学,friend-id:user-123,add-time:2025-01-15;friend-nickname:李四,friend-remark:同事,friend-id:user-456,add-time:2025-01-16

# 多个群成员存储示例：
member-id:user-001,member-nickname:张三,join-time:2025-01-10,member-role:member;member-id:user-002,member-nickname:李四,join-time:2025-01-11,member-role:admin
```

### 查询示例

```sql
-- 查询用户的所有好友
SELECT "user-id", "user-owned-firends" 
FROM "users" 
WHERE "user-id" = 'user-001';

-- 查询群聊的所有成员
SELECT "group-id", "group-name", "group-members" 
FROM "groups" 
WHERE "group-id" = 'group-001';
```
